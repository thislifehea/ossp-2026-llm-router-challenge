# 정책 실험 로그

`.claude/skills/router-policy-experiment/SKILL.md` 절차로 `ossp_router.cli self-check`를
돌린 결과를 실험할 때마다 한 줄씩 추가한다. 공식 채점 하네스(`src/ossp_router/scoring.py`)를
그대로 쓰므로 여기 적힌 수치는 (materialize된 데이터 기준이라면) 실제 채점 방식과 동일하다 —
단 채점용 비공개 평가셋과 공개 Dev는 분포가 다를 수 있으니 "공개 Dev 대비 baseline과의
방향"으로 우선 읽을 것.

공식 baseline(참고용, 우리 실험 아님) 4종은 실험ID 없이 `—`로 표기한다. 우리가 직접 돌린
실험은 시간순으로 A부터 알파벳을 붙인다. **한 행 = 한 실험(하나의 정책·하나의 설정)만
기록한다** — 여러 설정을 한 번에 돌린 그리드/스윕은 설정마다 별도 행으로 쪼개고, 원래
로그에 tier별 세부 수치가 안 남아있는 조합은 `final_score`만 채우고 나머지 칸은 `—`로
비워둔다(숫자를 지어내지 않는다).

`SOTA` 열은 **현재 실제 제출 컨테이너에 채택된 행**에 ⭐를 붙인 것 — raw `final_score`가
가장 높은 행이 아니라 "로버스트니스 검증(리샘플 10/10)까지 통과해서 실제로 쓰고 있는
정책"을 뜻한다(2026-08-20부로 정의 변경, 그 전까지는 raw 최고점 기준이었음 — 아래 발견
이후 채택 판단 방식 자체가 raw 최댓값에서 로버스트니스 우선으로 바뀐 것과 같은 이유).
raw `final_score`가 SOTA보다 높은 행이 있을 수 있는데(예: N, 아래 참고), 그런 행은 리샘플
검증에 실패해 실제로는 못 쓰는 값이라는 뜻이니 비고에서 사유를 확인할 것.

**2026-08-19 중요 발견(실험P/Q 참고)**: Dev 880문항 "그 정확한 조합"에서 나온 raw
`final_score` 1등이 실제로는 안정적이지 않을 수 있다는 게 드러났다 — Dev 880을 무작위로
반씩(440개) 10번 다르게 쪼개서 같은 safety_ratio를 적용해봤더니, N(당시 raw 1등,
0.704659)이 Balanced/Premium에서 절반 넘게 예산을 못 맞추고 0점 처리됐다. 이후로는
tier_score 최댓값보다 **"리샘플한 서브셋에서도 예산을 지키는지"를 먼저 걸러내고, 그 안에서
점수를 비교**하는 방식(로버스트니스 우선 선택)을 쓴다 — N의 raw 최고점(0.704659)은
역대 최댓값 "기록"으로 남지만, SOTA는 아니다.

| 실험ID | 날짜 | 정책 | split | Fast (score/budget_ratio) | Balanced | Premium | final_score | SOTA | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| — | — | `baselines/always_light.py` | dev | 0.619318 / 1.000000 | 0.619318 / 1.000000 | 0.619318 / 1.000000 | 0.619318 | | 공식 baseline (참고, `baselines/README.md`) |
| — | — | `baselines/prompt_heuristic.py` | dev | 0.625852 / 1.072334 | 0.658239 / 1.367866 | 0.691761 / 2.102044 | 0.655341 | | 공식 baseline (참고) |
| — | — | `baselines/feature_budget.py` | dev | 0.621023 / 1.038210 | 0.623580 / 1.334059 | 0.691761 / 2.102044 | 0.643011 | | 공식 baseline (참고) |
| — | — | `baselines/hash_regex.py` | dev | 0.663068 / 1.235989 | 0.693750 / 1.961506 | 0.740057 / 3.985205 | 0.695369 | | 공식 baseline (참고, 최초 목표 기준선) |
| A | 2026-08-12 | 실험A: hash_regex, hash_bins 256→1024 (재학습) | dev | 0.655682 / 1.249820 | 0.676420 / 1.973803 | 0.726136 / 3.965749 | 0.683040 | | baseline보다 전 tier 악화(-0.0123 final). 차원(1038)이 Train 샘플 수(1760) 대비 과대해져 ridge(alpha=100 자동선택)로도 과적합/노이즈 못 잡은 것으로 추정. **채택 보류.** (원인 진단은 실험C에서 정정됨 — 좁은 alpha 그리드 문제였음) |
| B | 2026-08-12 | 실험B: hash_regex 공식 아티팩트, premium safety_ratio만 0.925→0.897(×0.97) | dev | 0.663068 / 1.235989 (fast/balanced 원본 그대로) | 0.693750 / 1.961506 | 0.737216 / 3.825778 | 0.694517 | | final은 baseline 대비 -0.00085로 거의 동일, **premium 예산 여유는 3.985→3.826로 유의미하게 확보**(한도 4.0에 근접했던 리스크 완화). quality 손실 대비 안전마진 이득이 커 보임 — **채택 후보**(이후 H가 같은 기법을 Fast에 적용해 실제 채택). |
| C | 2026-08-12 | 그리드스윕(`build/sweep/run_sweep.sh`, hash_bins∈{32,64,128,256,512,1024}×alpha∈{0.1,1,10,30,100,300,1000}) 중 **hash_bins=256, alpha=1000** | dev | 0.665341 / 1.247551 | 0.696591 / 1.958082 | 0.745739 / 3.940048 | 0.698835 | | 그리드 42조합 중 최고(baseline+0.0035). 실험A의 "차원 늘리면 무조건 나빠짐" 결론은 **틀렸음** — 원인은 alpha 그리드가 좁았던 것(최대 100), alpha=1000까지 넓히니 이 조합이 이김. 단 **Fast 예산 여유가 0.20%뿐**(한도 1.25 중 1.2476)이라 비공개셋에서 초과 위험 큼. 공식 CLI 재검증·채택은 D, Fast 여유 보완은 H 참고. 같은 스윕의 다른 두 조합은 C2/C3. 42조합 원본 수치는 `build/sweep/results.csv`(git 비추적, 로컬 산출물). |
| C2 | 2026-08-12 | 그리드스윕(실험C와 동일 스윕) 중 **hash_bins=512, alpha=1000** | dev | 0.663068 / 1.243516 | 0.697727 / 1.973117 | 0.738636 / 3.718822 | 0.696136 | | baseline 소폭 상회(+0.0008). final_score는 C보다 낮지만 세 tier 예산 여유가 더 고르게 남음(0.52%/1.34%/7.03%) — 안전한 절충안 후보였으나 별도 채택 없이 bins=256 계열(C→D→H)이 채택으로 진행됨. |
| C3 | 2026-08-12 | 그리드스윕(실험C와 동일 스윕) 중 **hash_bins=1024, alpha=1000** | dev | 0.668750 / 1.200931 | 0.701989 / 1.992063 | 0.732102 / 3.974235 | 0.697727 | | baseline 상회(+0.0024), Fast 예산 여유는 셋 중 가장 큼(3.93%)이지만 Premium 여유는 얇음(0.64%). 채택 안 함. |
| D | 2026-08-13 | **채택(중간)**: bins=256/alpha=1000 (`artifacts/hash-regex-bins256-alpha1000.v1.json`), 공식 `ossp_router.cli self-check`로 재검증 완료 | dev | 0.665341 / 1.247552 | 0.696591 / 1.958082 | 0.745739 / 3.940048 | 0.698835 | | 공식 CLI로 실험C의 그리드스윕 수치 재현 확인(오차 없음, C와 동일 조합의 공식 검증본). **Fast 예산 여유 0.20%(⚠️ 위험)로 알고 채택** — raw 점수 우선 판단. 아티팩트를 `artifacts/`에 저장(공식 `baselines/`와 구분). 이후 H에서 이 Fast 여유 문제를 보완하며 안전마진 우선으로 최종 채택이 넘어감(현재 채택은 H). raw 최고점 기록은 이후 K(릿지+GBM 앙상블)로 넘어감. |
| E | 2026-08-13 | 실험E: `baselines/feature_cluster.py`, hash_regex 270차원 특징을 K=32 K-means로 클러스터링 (진짜 의미 임베딩 대신 — 이 환경 torch DLL 로드 실패로 대체) | dev | 0.644318 / 1.107009 | 0.688920 / 1.630718 | 0.711080 / 2.930824 | 0.677727 | | 지금까지 중 최저(-0.0211 vs baseline). **클러스터 크기 min=1, max=306으로 심하게 불균형** — 270차원 signed hash 벡터에 순수 유클리드 K-means가 잘 안 맞음(고차원 sparse 벡터 특유의 거리 붕괴 문제로 추정). 예산 여유는 넉넉해졌지만(Fast 12.5%/Premium 27%) 품질이 그만큼 다 깎여서 "안전하지만 부정확"해짐. **채택 안 함.** PCA 전처리(실험F) 또는 실제 의미 임베딩으로 재시도할 가치 있음. |
| F | 2026-08-13 | 실험F: `baselines/feature_cluster_pca.py`, hash_regex 270차원 특징을 PCA(24차원)로 축소 후 K=32 K-means (`exp-pca-cluster` worktree, `parallel-policy-sweep` 절차) | dev | 0.648295 / 1.207432 | 0.686080 / 1.527949 | 0.726989 / 3.710696 | 0.683239 | | 실험E의 클러스터 불균형(min=1/max=306)은 PCA로 해소됨(min=15/max=125, 평균 55) — 가설은 맞았지만 baseline(0.695369)도 못 넘음(-0.0121). 실험E(0.677727) 대비는 +0.0055 개선. **채택 안 함.** hash 기반 signed 벡터 자체가 유클리드 거리 기반 방법과 근본적으로 안 맞을 가능성 — 진짜 의미 임베딩(torch 문제 해결 후) 필요해 보임. 코드는 `exp-pca-cluster` 브랜치에만 있고 메인 미머지. |
| G | 2026-08-13 | 실험G: `baselines/knn_router.py`, k-최근접이웃(k∈{10,15,20,25}) score/cost 예측 중 **k=15**(`exp-knn` worktree, `parallel-policy-sweep` 절차) | dev | 0.653693 / 1.228987 | 0.686932 / 1.994674 | 0.719602 / 3.952836 | 0.683438 | | baseline(0.695369) 미달. 클러스터 불균형 문제는 원천적으로 없는데도(모든 예측이 정확히 k개 이웃 평균) 안 됨 — **실험F와 교차검증: 거리 기반 방법(K-means/k-NN) 자체가 이 특징 공간에 안 맞고 ridge regression(좌표축 가중합)이 더 적합하다는 결론.** **채택 안 함.** 같은 그리드의 다른 k값은 G2/G3/G4(원 로그에 tier별 세부수치가 안 남아있어 final_score만 기록). 코드는 `exp-knn` 브랜치에만 있고 메인 미머지. |
| G2 | 2026-08-13 | 실험G와 동일 스윕 중 **k=10** | dev | — | — | — | 0.683693 | | tier별 세부 수치는 원 로그에 남아있지 않음(final_score만 기록). baseline 미달, 결론은 G 참고. |
| G3 | 2026-08-13 | 실험G와 동일 스윕 중 **k=20** | dev | — | — | — | 0.684602 | | tier별 세부 수치는 원 로그에 남아있지 않음(final_score만 기록). baseline 미달, 결론은 G 참고. |
| G4 | 2026-08-13 | 실험G와 동일 스윕 중 **k=25** | dev | — | — | — | 0.683438 | | tier별 세부 수치는 원 로그에 남아있지 않음(final_score만 기록). baseline 미달, 결론은 G 참고. |
| H | 2026-08-13 | 실험H: 채택 모델(`artifacts/hash-regex-bins256-alpha1000.v1.json`) Fast tier `safety_ratio` 마진 그리드(1.0/0.99/0.98/0.97/0.95/0.93) (`exp-fast-margin` worktree, `parallel-policy-sweep` 절차) | dev | margin=0.98: 0.658239 / 1.225672(여유 1.95%, 원본은 0.20%) | 0.696591 / 1.958082 (fast만 바꿔서 불변) | 0.745739 / 3.940048 (불변) | 0.695994 | | **채택(현재).** margin_factor 그리드 중 0.98이 최적 절충 — quality -0.0028만 손해보고 Fast 예산 여유가 0.20%→1.95%(~10배)로 확보됨(0.99는 손실 -0.0014/여유 1.03%로 더 보수적인 대안, 0.97 이상은 여유 대비 손실이 급격히 커짐). raw `final_score`는 D/C(0.698835, 지금은 K=0.701591이 최고)보다 낮아 `SOTA` 표시는 없지만, Fast 예산 초과 리스크를 줄인 게 더 중요하다고 판단해 **이게 2026-08-14 기준 실제 채택 정책**(K 채택 여부는 별도 판단 대기). 부수 발견: 모델의 내부 예측 비용비율(1.216)이 실측(1.247)보다 3%가량 낮아서 애초에 여유가 얇았던 것으로 추정 — 이 가설은 K/L에서 GBM-릿지 앙상블 기준으로 훨씬 크게(최대 52%) 재확인됨. `artifacts/hash-regex-bins256-alpha1000.v1.json`에 직접 반영(fast safety_ratio 0.973333→0.953867), 공식 self-check로 재검증 완료(final_score 0.695994, baseline 0.695369 상회 유지). |
| I | 2026-08-14 | 실험I: `baselines/gbm_regex.py`, hash_regex의 270차원 특징 그대로 Gradient Boosted Trees(`sklearn.ensemble.GradientBoostingRegressor`, 학습 전용)로 score/cost head 6개 교체, 추론은 numpy 없이 순수 파이썬 트리 순회. select_models/fill_ax31_upgrades는 hash_regex와 동일 재사용 (`parallel-policy-sweep` 절차) | dev | 0.655682 / 1.245044 | 0.699432 / 1.936241 | 0.740909 / 3.996430 | 0.694375 | | baseline(0.695369)·H(0.695994) 둘 다 미달(-0.0016 vs H). k-NN/K-means(0.68대)보다는 확실히 나아서 "거리 기반보다 좌표 분할 방식이 이 특징 공간에 낫다"는 가설은 일부 확인됐지만 릿지를 못 넘음. 게다가 **Fast 여유 0.40%, Premium 여유 0.09%**로 안전성도 더 나쁨. **채택 안 함.** 아티팩트 `artifacts/gbm.v1.json`(worktree 로컬, 미커밋) — 이후 K에서 릿지와 앙상블할 때 재사용됨. |
| J | 2026-08-14 | 실험J: `baselines/train_glm_cost.py`, score head는 기존 릿지 그대로, **cost head만** log-link Gamma GLM(`sklearn.linear_model.GammaRegressor`, 학습 전용)으로 교체 — 아티팩트 스키마가 hash_regex와 동일해서 추론은 `hash_regex.py` 그대로 재사용 (`parallel-policy-sweep` 절차) | dev | 0.663636 / 1.247788 | 0.697727 / 1.982185 | 0.742614 / 3.926712 | 0.697557 | | raw 점수는 H(0.695994) 대비 +0.0016로 이김. 근데 **Fast 예산 여유가 0.18%**로 예전 D(0.20%)와 똑같이 위험한 수준 — H처럼 안전마진 보정을 추가로 해야 공정 비교가 되는 미완성 상태. **보류(추가 안전마진 튜닝 없이는 채택 판단 불가).** 아티팩트 `artifacts/glm-cost.v1.json`(worktree 로컬, 미커밋). |
| K | 2026-08-14 | 실험K: `baselines/ensemble_regex.py` — I(GBM)과 기존 채택 릿지(H)의 문항별 예측 품질·비용을 단순 평균(mean ensemble)한 뒤, 동일한 라그랑주 선택 로직으로 라우팅. safety_ratio만 tier별로 재그리드서치(60분할), 특징/모델 재학습 없음 | dev | 0.663068 / 1.204775 | 0.703977 / 1.979156 | 0.750568 / 3.977879 | 0.701591 | | 예산 여유도 D(0.20%)·J(0.18%)·I(0.09%)보다 확연히 건강함(Fast 3.6% / Balanced 1.0% / Premium 0.55%) — 두 독립 모델(릿지·GBM)을 평균 내면서 각자의 예측 노이즈가 일부 상쇄된 것으로 추정. raw 최고점 기록·SOTA 표시는 이후 N으로 넘어감. **2026-08-14~18: 실제 제출 컨테이너에 채택·통합**(이후 2026-08-18에 N으로 대체됨) — `src/ossp_router/routing_ensemble.py`(hash_regex.py+gbm_regex.py+ensemble_regex.py를 의존성 없이 포팅, 두 아티팩트는 `src/ossp_router/resources/`에 번들), `container/entrypoint.py`가 이걸 실행하도록 교체, `.dockerignore`/`tests/test_repository_policy.py` 갱신. linux/arm64·amd64 두 아키텍처로 이미지 빌드 확인, amd64는 실제 컨테이너(`--read-only --user 65532:65532 --network none`)로 Dev 880문항 라우팅까지 실행해 self-check 재현(final_score 0.701591, 네이티브 파이썬 실행과 완전 동일 — Fast tier 출력 파일 바이트 단위 일치 확인). arm64 이미지는 빌드까지만 확인(이 환경은 arm64 QEMU `docker run` 에뮬레이션 미지원이라 실행 검증은 amd64로 대체 — 파이썬 순수 코드라 아키텍처 의존 없음). `tools/check_runtime.py`(자원/시간 한도 정밀검증)는 이 Windows 환경에 `fcntl` 모듈이 없어 미실행 — 제출 전 Linux 환경에서 한 번 더 돌려볼 것. Balanced/Premium 예산 여유가 아주 넉넉하진 않다는 점(1.0%/0.55%)은 남아있는 리스크로 인지. |
| L | 2026-08-14 | 실험L: K(릿지+GBM 앙상블)의 비용 예측에 Train 기준 모델별 편향보정계수(실측합/예측합 = light 1.271× / ax31 1.193× / think 1.518×, 옌센 부등식에 의한 log-cost 재변환 편향)를 곱해서 재보정 후 safety_ratio 재그리드서치 | dev | 0.663636 / 1.215863 | 0.703409 / 1.979800 | 0.742045 / 3.795988 | 0.699091 | | **K보다 -0.0025 낮음 — 가설(편향 보정이 안전마진보다 근본적)은 방향은 맞았지만 단순 구현으로는 역효과.** think의 편향(1.518×)이 워낙 커서 보정 후 최적화 단계가 think를 실제보다 과대평가된 비용으로 취급 → Premium에서 필요한 문항에도 think 선택을 과하게 아껴 예산 여유만 5.1%로 남기고 품질을 손해봄(0.7506→0.7420). Train 전체 합계 기준 단일 보정계수가 너무 뭉뚱그려진 도구였던 것으로 추정 — 문항별/선택된 부분집합별로 다른 보정이 필요해 보이나 미구현. **채택 안 함.** |
| M | 2026-08-18 | 실험M: `baselines/train_embedding_gbm.py`+`embedding_gbm_regex.py` — 정규식 dense 14개 + 다국어 문장임베딩(`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, 384차원) 하이브리드 특징에 GBM(`GradientBoostingRegressor`, K-fold OOF로 하이퍼파라미터 선택) 적용. "특징과 모델을 매칭된 쌍으로 같이 바꿔야 한다"는 가설을 테스트하는 마지막 미시도 조합(임베딩+릿지는 이미 시도해 0.686023, hash+GBM도 이미 시도해 0.694375) | dev | 0.653693 / 1.238298 | 0.692898 / 1.928913 | 0.728409 / 3.831074 | 0.687869 | | baseline(0.695369)·H(0.695994)·K(0.701591) 전부 미달. 임베딩+릿지(0.686023)보다는 +0.0018 소폭 나아서 "이 임베딩 특징엔 GBM이 릿지보다 살짝 낫다"는 방향성은 있지만, hash+regex 특징에서는 반대로 릿지가 GBM을 이겼던 것(H>I)과 정반대 패턴이라 "모델을 특징에 맞춰 짝지어야 한다"는 가설을 뒷받침하지 않음 — 오히려 **어떤 모델을 얹든 이 임베딩 표현 자체가 hash+regex보다 이 데이터셋엔 안 맞는다**는 기존 결론(임베딩-hybrid 실험)을 재확인. 학습에 필요한 임베딩은 이전 실험(중단된 에이전트)이 남긴 캐시(`build/embedding-cache/*.npy`, 현재 materialized 입력과 SHA-256 일치 확인됨)를 재사용 — 이 환경은 torch DLL 로드가 여전히 안 돼(E/F 때와 동일 증상, `.venv-embed`에 `torch==2.13.0+cpu`가 설치는 되어 있으나 import 자체가 실패) 새 임베딩 계산은 불가능했음. **채택 안 함.** 임베딩 계열 접근은 이것으로 일단 막다른 길로 판단. |
| N | 2026-08-18 | 실험N: `baselines/train_quantile_cost.py` — score head는 K와 동일(릿지+GBM 평균, 재학습 없음), **cost head를 GBM quantile regression으로 교체**(`GradientBoostingRegressor(loss="quantile")`, hash+regex 270차원, alpha∈{0.55~0.95} 그리드). log(비용)의 분위수는 exp() 단조변환에서 보존되므로 그대로 실제비용의 분위수가 됨 — tier마다 (alpha, safety_ratio) 조합을 함께 그리드서치(alpha는 tier별 예산 다급함에 맞춰 다르게 선택되도록 허용) | dev | 0.665625 / 1.175768 (alpha=0.75) | 0.705682 / 1.975586 (alpha=0.65) | 0.755682 / 3.979817 (alpha=0.65) | **0.704659** | | **지금까지 기록된 것 중 raw 최고점**(K 대비 +0.0031, SOTA는 아님 — 아래 참고). 안전계수를 tier 전체에 균일하게 곱해 깎는 대신, cost head 자체가 "이 문항은 85%tile 기준 비용이 얼마"를 예측하도록 학습해서 **문항별 불확실성이 예측값에 자동으로 녹아듦**(출력 길이가 들쭉날쭉한 axk1-think류 문항은 자동으로 더 큰 여유를 갖고, 예측 가능한 문항은 여유가 작음) — 획일적 safety_ratio보다 원리적으로 더 정교한 예산 마진 방식. 예산 여유도 K보다 더 건강함(Fast 3.6%→**5.9%**, Balanced 1.0%→1.2%, Premium 0.55%→0.50%). 흥미로운 부수 발견: tier별 최적 alpha가 달랐음(Fast=0.75 더 보수적, Balanced/Premium=0.65) — 예산이 빠듯한 tier일수록 더 높은(보수적인) 분위수가 유리하다는, tier 단위에서의 "상황별 마진" 신호. **2026-08-18: 실제 제출 컨테이너에 채택·통합 완료**(K 대체) — `src/ossp_router/routing_ensemble.py`에 `QuantileCostArtifact`/`_predict_quantile_cost` 추가, tier→alpha 매핑(`_TIER_COST_QUANTILE`), 아티팩트는 `src/ossp_router/resources/quantile-gbm-cost.v1.json`(9개 alpha 중 실사용 2개만 남기고 pruning, 1.0MB). `.dockerignore`/`tests/test_repository_policy.py` 갱신. linux/arm64·amd64 이미지 빌드 확인, amd64는 실제 컨테이너(read-only/non-root/network none)로 Dev 880문항 라우팅 실행까지 검증(self-check 재현 완전 동일, 출력 파일 바이트 단위 일치). 기존 테스트 회귀 없음. `tools/check_runtime.py` 정밀검증은 이 환경 fcntl 제약으로 여전히 미실행 — 제출 전 Linux 환경에서 한 번 더 필요. **2026-08-19 추가**: Dev 880을 반씩 10번 리샘플링해서 재검증했더니 Balanced가 8/10, Premium이 6/10만 예산을 통과함 — raw 1등(0.704659)이 이 정확한 880문항 조합에 맞춰진 값이었다는 뜻. 로버스트니스까지 반영한 후속 정책은 Q, **2026-08-19부로 컨테이너 채택이 Q로 넘어감**. |
| O | 2026-08-18 | 실험O: `baselines/train_conformal_cost.py` — N의 "그리드서치로 대충 맞춘 마진"을 **Conformalized Quantile Regression(CQR, Romano et al. 2019)**으로 대체 시도. Train을 fit(80%, 1408)/calib(20%, 352)로 분리, quantile-GBM을 fit에서만 학습(N과 동일 alpha 후보), calib에서 실측 log(비용)과의 잔차를 구해 "이 정도 보정을 더하면 수학적으로 (1−δ) 확률로 예산을 안 넘는다"를 보장하는 δ를 tier별로 그리드서치(alpha도 같이 서치). 1차 시도(원시 비용 스케일 가산 보정)는 think의 잔차 분산이 압도적으로 커서(p90=0.116, 최대 3.30) 과보정 → 로그 스케일(승산적) 보정으로 수정 | dev | 0.667330 / 1.232442 (alpha=0.65,δ=0.075) | 0.703409 / 1.982110 (alpha=0.65,δ=0.3) | 0.748295 / 3.748897 (alpha=0.75,δ=0.3) | 0.702443 | | **K(0.701591)는 살짝 이겼지만 N(0.704659)에는 -0.0022로 못 미침** — calib용으로 Train의 20%를 떼어주면서 quantile head 학습에 쓸 수 있는 표본이 1760→1408로 줄어든 게 손실 원인으로 추정(컨포멀 방법의 잘 알려진 대가: 통계적 보장을 얻는 대신 일부 효율을 희생). **다만 가치 있는 검증 결과**: 선택된 (alpha, δ) 조합의 커버리지를 Dev(학습에 전혀 안 쓰인 진짜 홀드아웃)에서 재확인했더니 이론적 보장(예: δ=0.075→92.5% 이상)과 실제 Dev 커버리지(91.1~94.3%)가 거의 일치 — **N이 그리드서치로 찾은 마진이 우연히 Dev에만 맞춰진 게 아니라 통계적으로 합리적인 수준이었다는 간접 검증**이 됨. **채택 안 함**(raw 점수가 N보다 낮음) — 다만 "제출 전 이 방법으로 N의 마진이 대체로 타당한지 재확인하는 용도"로는 유용. |
| P | 2026-08-19 | 실험P: K/N이 고정으로 쓰던 score 앙상블 비율(릿지:GBM = 50:50)을 tier별로 `w·릿지 + (1-w)·GBM` 그리드서치(재학습 없음, 기존 예측값만 재혼합). safety_ratio도 같이 재탐색 | dev(880, 검증 안 됨) | 0.670455 / 1.240977 (w=0.8) | 0.705682 / 1.975586 (w=0.5) | 0.756250 / 3.967913 (w=0.45) | 0.706762 | | **1차 결과만 보면 N(0.704659)보다 +0.0021로 raw 신기록**처럼 보였음(Fast는 릿지 비중을 0.8까지 높인 게, Premium은 GBM 비중을 살짝 높인 게 최적). 근데 Dev를 반씩 10번 리샘플링해서 검증했더니(실험N 마진이 표본에 약하다는 걸 발견한 바로 그 절차) **Fast의 새 설정(w=0.8)이 Dev-B(진짜 홀드아웃 절반)에서 예산 초과로 0점** — N의 기존 설정(w=0.5)은 같은 Dev-B를 통과했는데 "개선"이라던 설정이 오히려 덜 강건했음. **채택 안 함** — raw Dev-880 그리드서치만으로 고른 하이퍼파라미터가 다시 한번 안 믿을 만하다는 걸 확인(이 발견 이후로는 P처럼 raw 최댓값만 보지 않고 Q부터는 로버스트니스를 먼저 거르는 방식으로 절차를 바꿈). |
| Q | 2026-08-19 | 실험Q: `baselines/train_quantile_ridge_cost.py` — N이 cost head를 GBM quantile 단독으로 교체하며 포기했던 "K의 앙상블 원리(독립된 두 모델 평균→노이즈 상쇄)"를 cost 쪽에도 적용. `sklearn.linear_model.QuantileRegressor`(선형 분위수회귀, pinball loss+L1, 5-fold CV로 L1 alpha 선택)를 N과 같은 분위수(0.65/0.75)로 학습해서 기존 GBM quantile cost와 평균. **safety_ratio 탐색 절차도 변경**: Dev 880 raw 점수 최댓값이 아니라, **Dev를 10번 다르게 반씩 쪼갠 서브셋 전부(10/10) 예산을 통과하는 것을 1순위 조건으로 걸고, 그 안에서 Dev-880 tier_score를 최대화** | dev(10/10 서브셋 검증됨) | 0.668466 / 1.192773 | 0.696591 / 1.886672 | 0.746023 / 3.460724 | 0.700170 | | **같은 로버스트니스 기준(10/10)에서 GBM 단독(N을 이 절차로 재평가하면 0.698523)과 공정 비교하면 앙상블이 +0.0016~0.0017 승** — Fast·Premium은 뚜렷이 개선, Balanced는 오차범위(-0.0006). K가 score에서 찾은 "두 모델 평균 = 노이즈 상쇄" 효과가 cost에서도 재현됨. raw `final_score`만 보면 N(0.704659)보다 낮아 보이지만, N은 표본을 흔들면 Balanced 8/10·Premium 6/10로 자주 깨졌던 값이라 직접 비교가 부당함(그래서 SOTA ⭐는 그대로 N에 남겨두되 "raw 기록"이라는 단서가 붙음, 문서 상단 2026-08-19 안내 참고). **채택.** 이 실험이 사실상 실험 우선순위 1(N 마진 보강)과 2(cost 앙상블)를 동시에 해결. `src/ossp_router/routing_ensemble.py`에 `QuantileRidgeCostArtifact`/`_ridge_quantile_cost` 추가, `_predict_quantile_cost`가 GBM+릿지 평균을 반환하도록 변경, `_TIER_SAFETY_RATIOS`를 로버스트 값(fast 0.99→0.985, balanced 0.975→0.9125, premium 1.0→0.85)으로 교체. 아티팩트는 `src/ossp_router/resources/quantile-ridge-cost.v1.json`(선형모델이라 55KB, GBM 대비 훨씬 작음). `.dockerignore`/`tests/test_repository_policy.py` 갱신. linux/arm64·amd64 이미지 빌드 확인, amd64는 실제 컨테이너(read-only/non-root/network none)로 Dev 880문항 라우팅 실행까지 검증(self-check 재현 완전 동일 0.700170, 출력 파일 바이트 단위 일치). 기존 테스트 회귀 없음. **2026-08-19부로 실제 제출 컨테이너 채택 정책(N 대체).** `tools/check_runtime.py` 정밀검증은 여전히 미실행 — 제출 전 Linux 환경에서 필요. |
| R | 2026-08-19 | 실험R: O/Q의 컨포멀 보정을 "모델 하나당 보정치 하나"에서 **문항 단위(Mondrian conformal)로 세분화** — calib 352개를 모델별로 예측 비용 크기 기준 3분위(그룹당 약 117개)로 나눠 그룹마다 따로 δ 보정치 계산. score head/robustness 절차는 Q와 동일(10/10 서브셋 우선) | dev(10/10 서브셋 검증됨) | 0.666761 / 1.177565 (alpha=0.75,δ=0.075) | 0.698864 / 1.857436 (alpha=0.75,δ=0.1) | 0.746023 / 3.434597 (alpha=0.8,δ=0.2) | 0.700170 | | **Q와 final_score가 소수점까지 완전히 동일(0.700170454545)** — tier별로는 Fast는 Q가 조금 낫고 Balanced는 R이 조금 나은데 가중합에서 우연히 정확히 같은 값으로 수렴. 그룹을 세분화한 만큼(calib 352→그룹당 ~117) 보정치의 통계적 노이즈도 같이 커져서, "더 정교한 보정"의 이득과 "그룹당 표본이 작아진" 손실이 서로 상쇄된 것으로 추정 — 지금 갖고 있는 Train 크기(1,760)로는 이 이상 세분화해도 득이 없다는 신호. **채택 안 함**(Q와 동점인데 구조는 더 복잡 — 오컴의 면도날로 Q 유지). 이걸로 오늘 세운 우선순위 1(마진 보강)·2(cost 앙상블)·3(문항 단위 정교화) 전부 마무리, 최종 결론은 Q. |
| S | 2026-08-19 | 실험S: `baselines/train_quantile_score.py` — O/Q/R이 cost에만 적용했던 분위수 회귀를 **score(품질) head에도 적용**. GBM quantile(alpha∈{0.10~0.45}, 낮은=비관적 분위수)로 "이 정도는 확실히 보장되는 품질" 하한을 예측해서 mean(릿지+GBM) 대신 라그랑주에 투입, safety_ratio와 함께 그리드서치(200-trial 하네스로 만든 것과 별개로 이번엔 10-trial 스크리닝만 — S가 로버스트니스에서도 이겼어야 200-trial 정밀검증으로 넘어가는 구조) | dev(10-trial 스크리닝) | 0.668182 / 1.171971 | 0.694318 / 1.852759 | 0.746023 / 3.460724 | 0.699375 | | **8개 분위수 후보 전부 "mean(점추정)"에 졌음 — 3개 tier 다 mean이 로버스트니스·raw 점수 양쪽에서 우위.** cost에서 분위수 회귀가 통했던 건 "예산 초과=tier 전체 0점"이라는 비대칭 리스크(꼬리를 비관적으로 잡아야 유리) 때문이었는데, score는 그런 벼랑끝 구조가 없어서 "기대값을 최대화"하는 mean이 이미 최적이라는 게 확인됨 — 일부러 비관적으로 잡으면 순수 손해만 봄. **채택 안 함.** cost와 score의 비대칭성 차이를 명확히 확인한 유의미한 negative result. |
| T | 2026-08-19 | 실험T: `baselines/char_ngram_features.py` — hash_regex의 특징 표현 중 단어 1-2gram 해싱 블록만 **문자 3/4/5gram 해싱**(경계패딩 `<token>`, 동일 signed FNV1a64 256bin)으로 교체, 나머지(14개 dense 정규식 특징, 릿지 학습·`select_models`/`fill_ax31_upgrades`)는 전부 hash_regex.py 그대로 재사용. 한국어 조사/어미 변화(예: "이야기가" vs "이야기는")에 강건할 거란 가설. 릿지 단독(alpha=1000, 5-fold OOF로 선택 — 원래 단어 n-gram 릿지와 같은 alpha) 대 D/H와 같은 조건(단어 n-gram 릿지 단독)으로 로버스트니스 우선(10/10 서브셋) safety_ratio 그리드서치 비교 | dev(10/10 서브셋 검증됨) | 0.642898 / (safety=0.89) | 0.662784 / (safety=0.725) | 0.687216 / (safety=0.475) | 0.657759 | | **3개 tier 전부 단어 n-gram 릿지(H, 0.663068/0.696591/0.740057)에 뚜렷이 뒤짐**(각각 -0.020/-0.034/-0.053) — 문자 n-gram으로 바꾸면 로버스트니스 기준 최적 safety_ratio도 훨씬 낮아짐(예: premium 0.85→0.475), 즉 예측 자체의 신뢰도가 떨어져서 마진을 훨씬 더 크게 잡아야 그나마 예산을 지킴. 원인 추정: 문자 3/4/5gram은 어휘 다양성이 단어 1-2gram보다 훨씬 커서 256bin 해시 충돌이 심해지고, 이 데이터셋의 도메인 신호(코드/수식/형식적 추론 마커 등)는 애초에 dense 정규식 특징이 이미 잘 잡고 있어 문자 단위 형태소 강건성의 이득보다 해시 충돌 손실이 훨씬 큼. **채택 안 함** — 명확한 negative result라 hash_bins를 늘린 재시도(예: 1024) 없이 문자 n-gram 계열은 여기서 종료, 다음 항목(도메인 차이 확인)으로 이동. 코드는 worktree 로컬에만 있음(`baselines/char_ngram_features.py`, `artifacts/char-ngram-ridge.v1.json`), budget_ratio는 로버스트니스 그리드서치 산출물에 안 남아있어 safety_ratio만 병기. |
| U | 2026-08-19 | 실험U: MoE(전문가 혼합) 투자 여부를 판단하기 위한 **저비용 도메인 차이 확인**. Train 1,760개를 기존 dense 정규식 특징(code_marker_count/math_marker_count/`_FORMAL_REASONING`/`_MULTI_CONSTRAINT`/long_context)만으로 4개 도메인(code n=474, math_reasoning n=346, multi_constraint n=42, general 나머지 898)으로 규칙 기반 분류 → 모델별 평균 score 비교(1단계) 및 기존 채택 릿지(H)의 도메인별 잔차오차 비교(2단계) → **완전 분리 모델 대신** 도메인 one-hot(5차원)을 기존 270차원 특징에 추가해 릿지 재학습(alpha=1000, 동일 재학습 없는 부분 재사용 불가하므로 전체 재학습), H와 로버스트니스 우선(10/10) safety_ratio 그리드서치로 공정 비교 | dev(10/10 서브셋 검증됨) | 0.655114 / (safety=0.91) | 0.694318 / (safety=0.925) | 0.740057 / (safety=0.85) | 0.692358 | | **1단계(모델별 평균 score)**: code/math_reasoning은 light·ax31 점수가 크게 낮고(0.44~0.54) think와의 격차가 큼(0.23~0.33), general/multi_constraint는 세 모델이 훨씬 가까움(격차 0.08~0.12) — 도메인 신호는 실재함. **2단계(H의 도메인별 잔차오차)**: code/math_reasoning에서 light·ax31 잔차오차가 general보다 확실히 큼(0.34~0.41 vs 0.24~0.30) — 표면적으로는 도메인 특화 모델의 여지가 있어 보였음. **그런데 3단계(one-hot 추가 재학습) 결과, H(word_baseline: 0.655682/0.693182/0.739489) 대비 가중합 기준 +0.0003(사실상 잡음 수준, tier별로도 fast -0.0006/balanced +0.0011/premium +0.0006 — 방향조차 tier마다 다름)** — 도메인 정보를 명시적 특징으로 추가해도 이미 기존 14개 dense 정규식 특징(코드/수식/형식추론 마커 등)이 도메인 신호를 사실상 다 포섭하고 있었다는 뜻. **완전 분리형 MoE는 시도 안 함** — one-hot 추가만으로도 이득이 사실상 0인데, 완전 분리 모델은 오늘 P/N에서 이미 확인된 "표본이 작을수록 안전마진이 깨지기 쉽다"는 문제를 multi_constraint(n=42, 270차원 릿지에 턱없이 부족)에서 그대로 재현할 위험만 크고 얻는 게 없음. **채택 안 함, MoE 투자는 여기서 종료.** 코드는 worktree 로컬 인라인 스크립트로만 존재(파일 미생성), 아티팩트 `artifacts/domain-onehot-ridge.v1.json`. |
| V | 2026-08-19 | 실험V: C/D(hash_bins/alpha 그리드서치)를 오늘 확립한 로버스트니스 우선(10/10 서브셋) 방식으로 재탐색. hash_bins∈{128,256,512,1024}×alpha∈{100,300,1000,3000} 16조합, 각 조합마다 릿지 재학습(score/cost head, Q 파이프라인이 아닌 **릿지 단독** — GBM/quantile head는 재학습 안 함) 후 tier별 safety_ratio도 같이 그리드서치(13분할, 시간 절약을 위해 T/U에서 쓴 21분할보다 성기게), 10개 리샘플 서브셋 전부 통과를 1순위 조건으로 최댓값 선택 | dev(10/10 서브셋 검증됨, 16조합 전부) | bins1024/alpha1000: 0.663068 | 0.696307 | 0.721875 | 0.690682 | | **16조합 전부 10/10 통과** — hash_bins/alpha를 이 범위에서 어떻게 바꿔도 로버스트니스 자체는 깨지지 않음(오늘 다른 실험들과 달리, 여기선 "안전마진"이 변별 기준이 안 됨). 최고 가중합은 bins=1024/alpha=1000(0.690682), 근소한 차이로 bins=512/alpha=3000(0.690597)·bins=256/alpha=3000(0.689972) — 전부 현재 기본값 bins=256/alpha=1000을 이 그리드 안에서 재평가한 값(0.688835)보다 근소하게(+0.0009~0.0018) 높음. **단, 이 델타는 오늘 K/N/Q가 만든 개선폭(+0.003~0.006)보다 훨씬 작고, 릿지 단독 비교라 실제 프로덕션(Q, 릿지+GBM 앙상블+GBM/릿지 분위수 비용)에 그대로 적용하려면 GBM score head·GBM/릿지 quantile cost head 전부를 새 hash_bins로 재학습해야 함(현재 아티팩트 전부 256bin 전제)** — 앙상블 단계에서 두 모델의 예측 노이즈가 이미 상쇄되고 있어(K의 핵심 발견) 이 정도로 작은 특징표현 차이의 이득이 앙상블 이후에도 그대로 남을지 불확실. **채택 안 함** — 이득 대비 전체 파이프라인 재학습·재검증(Docker 재빌드 포함) 비용이 너무 큼. 오늘 합의한 5개 실험(부트스트랩 하네스/S/T/U/V) 전부 완료. |
| W | 2026-08-19 | 실험W: `baselines/bpe_features.py` — T(문자n-gram, 실패)와 H(단어n-gram, 채택)의 중간 지점 테스트. 사전학습 공개 BPE 토크나이저(`klue/roberta-base`, 한국어 코퍼스로 학습된 byte-level BPE, vocab 32,000, HuggingFace `tokenizers` 라이브러리로 로드 — torch 불필요, Windows DLL 문제 없이 설치됨)로 형태소 단위 서브워드 분리("이야기가"→"이야기"+"##가") 후 동일 signed FNV1a64 256bin 해싱(서브워드 1-2gram). 나머지(14개 dense 특징, 릿지 학습·라우팅)는 hash_regex.py 재사용. H와 로버스트니스 우선(10/10 서브셋) 방식으로 비교 | dev(10/10 서브셋 검증됨) | 0.653693 / (safety=0.96) | 0.685511 / (safety=0.925) | 0.706818 / (safety=0.7375) | 0.679176 | | **3개 tier 전부 H(word_baseline: 0.655682/0.693182/0.739489, 가중합 0.692075)에 뒤짐(-0.0129)** — 다만 T(문자n-gram, 가중합 0.657759, -0.0343)보다는 손실폭이 약 2.6배 작음. 가설대로 형태소 분리(BPE)가 순수 문자n-gram보다는 낫지만, 여전히 단어 전체를 해싱하는 것보다는 정보 손실이 커서 이 데이터셋·270차원 특징 조합에선 역효과. 서브워드 예시 자체는 의도대로 잘 나뉨(정성적으로는 성공)이라 "형태소 강건성" 가설 자체가 틀렸다기보단, 이 정도 규모(Train 1,760)·이 모델(릿지)에서는 단어 단위 해싱이 이미 충분히 잘 맞고 있어 서브워드로 더 세분화해봤자 이득보다 노이즈가 크다는 결론. **채택 안 함.** 코드는 `baselines/bpe_features.py`(worktree 로컬), 아티팩트 `artifacts/bpe-ridge.v1.json`. |
| X | 2026-08-20 | 실험X: 임베딩 재도전. 이전(M)까지 이 환경(`.venv-embed`, Windows)에서 torch DLL 로드가 계속 실패해 "죽은 에이전트가 남긴 캐시"만 재활용했던 문제를 **WSL(Ubuntu-22.04)에 별도 venv(`.venv-wsl-embed`)를 만들어 torch 2.13.0+cpu·sentence-transformers 6.0.0을 새로 설치**해서 우회 — 진짜 새로 계산한 임베딩(`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, 384차원)으로 재시도. M은 "임베딩으로 대체"였던 반면, 이번엔 **기존 dense14+hash256(270차원) 특징에 임베딩을 concat으로 추가**(654차원)해서 "대체가 아니라 보강"이 통하는지 별도로 검증(아직 안 해본 조합). 릿지 재학습(alpha=3000, 5-fold OOF 자동선택) 후 H와 로버스트니스 우선(10/10) 비교 | dev(10/10 서브셋 검증됨) | 0.651420 / (safety=0.89) | 0.691477 / (safety=0.975) | 0.730966 / (safety=0.8875) | 0.687301 | | **3개 tier 전부 H(0.655682/0.693182/0.739489, 가중합 0.692075)에 뒤짐(-0.0048)** — M의 순수 대체(가중합 기준 -0.008~-0.012)보다 손실폭은 작지만 여전히 negative. alpha가 기존 270차원 릿지(1000)보다 훨씬 큰 3000으로 선택된 것도 "추가된 384차원이 새로운 유효신호보다는 노이즈에 가까워 더 강하게 눌러야 했다"는 신호. **이번 실험으로 "torch/캐시 문제 때문에 임베딩이 저평가된 것 아니냐"는 가능성이 완전히 배제됨** — 순수릿지(M 이전)·순수GBM(M)·대체(M)·보강(X) 네 가지 조합 전부 이 데이터셋(Train 1,760, hash+regex 특징이 이미 도메인 신호를 잘 잡음)에서 일관되게 hash+regex 단독보다 손해를 봄. **채택 안 함, 임베딩 계열은 이걸로 최종 종결.** 코드는 `build/embedding-fresh/{compute_embeddings.py,train_hybrid.py}`(worktree 로컬), 아티팩트 `artifacts/hybrid-dense-hash-embedding-ridge.v1.json`, 임베딩 원본은 `build/embedding-fresh/{train,dev}_embeddings.npy`(모두 gitignore). |
| Y | 2026-08-20 | 실험Y: **decision-focused (policy-gradient 계열) 학습**. 지금까지(H~X) 전부 "score/cost를 MSE로 각각 회귀 → 별도로 라그랑주 최적화"하는 2단계 파이프라인이었던 것과 달리, **H의 릿지 가중치로 warm-start한 뒤 공식 채점 목적함수(tier별 가중 score − μ·cost, μ는 예산 상한에 맞춰 이분탐색)를 온도 어닐링 소프트맥스로 미분가능하게 완화**해서 직접 fine-tuning. Train은 반사실 정보(3개 모델 전부의 실제 score/cost)를 완전관측하고 있어서 REINFORCE 샘플링 없이 정확한 그래디언트 계산 가능(순수 numpy로 그래디언트 직접 유도, Adam 직접 구현). 총 3차 반복: **v1**(μ를 warm-start 시점에 1회만 고정) → **v2**(μ 주기적 재보정+Train 90/10... 80/20 홀드아웃으로 공식 채점함수 기준 조기종료 추가) → **v3**(μ 재보정을 EMA+step당 변화폭 클램프로 안정화, fit/val을 90/10으로 조정해 학습 데이터 확대) | dev(v1/v2는 10/10 서브셋 검증, v3는 Dev 평가 불필요 — 이유는 비고) | v1(버그, 1차): 0.619602 / v1(수정): 0.623295 / v2(best ep80): 0.651989 | v1(버그): 0.619602 / v1(수정): 0.649148 / v2: 0.671875 | v1(버그): 0.635795 / v1(수정): 0.656534 / v2: 0.673580 | v2 가중합 0.664432 (H 0.692075 대비 -0.0276) | | **3단계 전부 H를 못 넘김, 그리고 진단할수록 결론이 더 확실해짐.** v1 최초 시도는 버그(학습 루프에서 `cost_hat/light_avg`를 썼는데 μ는 `bisect_mu`에서 `cost_hat/light_total`로 캘리브레이션함 — light_avg가 light_total의 1/1760이라 **비용 페널티가 의도보다 ~1760배 과대적용**돼 정책이 거의 always-light로 붕괴, fast/balanced tier_score가 almost-light 베이스라인(0.619318)과 거의 같아짐 — 공교롭게도 이게 BudgetMem 논문이 경고한 "reward-scale alignment 없으면 정책이 무조건 LOW로 퇴화" 현상의 실제 재현이었음). 정규화를 통일해 버그를 고치니(v1 수정) 안전마진 없이 예산을 정확히 쓰면서도(safety=1.0) 여전히 H에 뒤짐 — 3개 tier 격차는 좁혀졌지만 해소는 안 됨. **v2**: μ가 학습 내내 고정이면 가중치가 바뀌는데 제약 캘리브레이션이 안 따라간다는 구조적 문제를 짚어 40 epoch마다 재보정(dual ascent) 추가, Train 80/20 내부 홀드아웃을 만들어 20%는 실제 공식 `score_submissions`로 조기종료 판단에 사용 — epoch 80에서 warm-start(0.6597) 대비 +0.0063 개선된 체크포인트를 찾았고 Dev에서도 3개 tier 전부 v1보다 개선(가중합 0.664)됐지만 **여전히 H 미달**, 게다가 epoch 900대에 μ가 재보정마다 널뛰다 2^40까지 폭주해 loss가 -0.2대에서 +97억까지 발산(조기종료가 이 발산 구간을 걸러내 최종 결과엔 영향 없었음). **v3**: 그 발산을 μ EMA(0.7:0.3 블렌드)+step당 변화폭 [0.5x,2x] 클램프로 완전히 해결(loss가 -0.19~-0.21 구간에서 안정적으로 유지)하고 fit 데이터도 1,408→1,584로 늘렸는데, **최고 체크포인트가 epoch 1(=warm-start=H 그 자체)로 나옴 — 1,200 epoch 내내 단 한 번도 H보다 나아진 적이 없음**(val_weighted가 0.6872에서 시작해 이후 전부 0.628~0.662로 더 낮음). 즉 v2의 "일시적 개선"은 아직 안정화되지 않은 μ 재보정이 만든 잡음/우연이었을 가능성이 크고, **안정화된 상태에서는 decision-focused 손실이 이 데이터 규모(Train 1,760, 270차원 선형모델)에서 MSE보다 나은 방향을 전혀 못 찾는다**는 게 가장 신뢰도 높은 결론 — v3는 최고점이 곧 출발점이라 Dev 재평가가 무의미해서 생략함. 원인 추정: decision-focused 손실은 결정 경계 근처에서만 그래디언트 신호를 주는 sparse한 손실인 반면 MSE는 전 구간에서 조밀한 신호를 줘서, 이 정도로 데이터가 적은(1,760개, 270차원) 선형모델 체제에서는 조밀한 지도학습 신호 쪽이 일반화가 더 잘 됨. **채택 안 함.** 코드는 `build/decision-focused/{train.py,train_v2.py,train_v3.py}`(worktree 로컬), 아티팩트 `artifacts/decision-focused-ridge{,-v2,-v3}.v1.json`. |
| Z | 2026-08-20 | 실험Z: **6개 헤드(score×3, log-cost×3) 공유 은닉층 MLP**. 지금까지(H/I/K/N/Q/Y) 전부 6개 헤드를 완전히 독립적으로 학습(릿지·GBM 각각 또는 그 앙상블)했는데, "이 문항이 어렵다"는 잠재 신호가 6개 헤드 모두에 공통으로 영향을 준다는 가설로 공유 은닉층+6개 선형출력 구조(`sklearn.neural_network.MLPRegressor`, 다중출력 회귀 기본 지원) 시도. hash+regex 270차원 특징(H와 동일, 비교용) 표준화 후 X/Y 둘 다 z-score, hidden_layer_sizes∈{(32,),(64,),(128,),(32,16),(64,32)}×alpha(L2)∈{0.001,0.01,0.1,1.0} 20조합을 4-fold CV로 그리드서치(목적함수는 릿지 alpha선택과 동일한 정신: score_mse+0.05×cost_mse 가중), H와 로버스트니스 우선(10/10) 비교 | dev(10/10 서브셋 검증됨) | 0.648580 / (safety=0.91) | 0.663920 / (safety=0.75) | 0.684659 / (safety=0.475) | 0.664006 | | **3개 tier 전부 H(0.655682/0.693182/0.739489, 가중합 0.692075)에 뒤짐(-0.0280)** — decision-focused(Y v2)의 손실폭과 비슷한 수준. 그리드서치 단계에서부터 경고 신호가 있었음: 선택된 조합(hidden=(32,16), alpha=1.0)의 CV 손실(2.8885)이 "항상 평균만 예측"하는 기준선(표준화된 타깃이라 이론상 3.15)보다 겨우 8% 정도만 나은 수준이었음 — MLP가 릿지보다 표현력은 크지만 Train 1,760개·270차원 규모에서 그 표현력을 살릴 만큼 데이터가 충분치 않아 강한 L2(alpha=1.0)로 정규화될 수밖에 없었고, 결과적으로 릿지보다 못한 지점에 수렴한 것으로 추정. `early_stopping=True`(내부 검증 15%) 때문에 학습이 비교적 일찍(`n_iter_=76`) 멈춘 것도 원인일 수 있음(early stopping 끄고 직접 모니터링하거나 hidden 확장하는 추가 라운드는 시도 안 하고 여기서 종료하기로 결정). **채택 안 함.** 코드는 `build/shared-mlp/train.py`(worktree 로컬), 아티팩트 `artifacts/shared-mlp.v1.json`. |
| AA | 2026-08-20 | 실험AA: **dense 정규식 특징 확장**(14개→22개). `DATA_LICENSES.md`의 공개 데이터 소스 구성(Belebele Korean/HRMCR/GSM8K/DeepMind Mathematics/AIME/TruthfulQA/RuleTaker/CRUXEval/BABILong)을 참고해 도메인을 더 세밀히 구분할 저비용 정규식 신호 8개 추가: 선택지 마커 개수(`log_choice_marker_count`), 이진답변 신호(`binary_answer_marker`, true/false/참/거짓), 방정식 밀도(`equation_density`, `=` 개수/길이), 코드 시그니처 개수(`log_code_signature_count`, def/class/return/->), 규칙 함의 개수(`log_rule_implication_count`, if...then/→), 경시수학 마커(`competition_math_marker`, find the number/how many/구하시오), 고유명사 밀도(`proper_noun_density`, BABILong류 스토리 캐릭터 신호), 물음표 밀도(`question_mark_density`). 나머지(256bin 해시, 릿지 학습·라우팅)는 hash_regex.py와 동일, H와 로버스트니스 우선(10/10) 비교 | dev(10/10 서브셋 검증됨) | 0.654261 / (safety=0.91) | 0.692614 / (safety=0.925) | 0.738636 / (safety=0.85) | 0.691079 | | **3개 tier 전부 H(0.655682/0.693182/0.739489, 가중합 0.692075)와 사실상 동률(-0.0010, 방향만 음수)** — safety_ratio도 3개 tier 전부 H와 완전히 동일한 값이 선택됨. 실험U(도메인 one-hot 추가해도 이득 +0.0003, 잡음 수준)와 정확히 같은 패턴 재확인: **기존 14개 dense 특징이 이미 도메인 신호를 충분히 포섭하고 있어서, 정규식 신호를 더 정교하게 늘려도 얻는 게 거의 없음**(오히려 차원만 늘고 유효 신호는 안 늘어 정규화가 더 세게 걸리는 효과로 미세하게 음수). **채택 안 함** — dense 특징 확장 방향은 여기서 종료. 코드는 `baselines/dense_features_v2.py`, 아티팩트 `artifacts/extended-dense-ridge.v1.json`. |
| BB | 2026-08-20 | 실험BB: **TF-IDF+SVD(LSA) 잠재의미 벡터를 기존 dense+hash 특징에 concat 보강**. M/X(sentence-transformers 사전학습 외부 임베딩)와 달리 이건 **Train 코퍼스 자체에서 통계적으로 학습한 표현**(`TfidfVectorizer(max_features=20000, sublinear_tf=True)` → `TruncatedSVD(n_components=64)`, explained_variance_ratio 합 0.353) — 도메인 미스매치 우려가 없는 다른 종류의 "임베딩"이라는 가설. hash_regex 270차원에 SVD 64차원을 더해 334차원 릿지 재학습(alpha=1000), H와 로버스트니스 우선(10/10) 비교 | dev(10/10 서브셋 검증됨) | 0.648580 / (safety=0.88) | 0.691193 / (safety=0.9) | 0.721875 / (safety=0.7375) | 0.683353 | | **3개 tier 전부 H(가중합 0.692075)에 뒤짐(-0.0087)** — sentence-transformers 하이브리드(X, -0.0048)보다도 조금 더 나쁨. 이걸로 서로 완전히 다른 두 종류의 "임베딩"(사전학습 의미모델 vs 코퍼스 자체 통계적 잠재의미)이 **순수릿지/순수GBM/대체/보강 조합까지 합쳐 총 5가지 방식으로 전부 일관되게 hash+regex 단독보다 손해**를 봄 — 이 데이터셋·이 특징공간에서는 어떤 형태의 "의미 표현" 보강도 도움이 안 된다는 결론이 훨씬 더 견고해짐. **채택 안 함, 임베딩·의미표현 계열 탐색은 이걸로 완전히 종결.** 코드는 `build/tfidf-svd/train.py`(worktree 로컬). 아티팩트(`artifacts/tfidf-svd-hybrid-ridge.v1.json`, 30MB — TF-IDF vocab 2만개+IDF+SVD 컴포넌트가 대부분 차지)는 negative result라 커밋하지 않음(`random_state=0`으로 재실행 시 재현 가능). |
| CC | 2026-08-20 | 실험CC: **도메인 게이팅 블렌딩**. Z(공유 MLP)·X/BB(임베딩)가 "표본 대비 표현력 과다"로 실패했다는 진단에 착안해, 정반대로 파라미터를 극도로 적게(도메인 수=5개) 추가하는 시도. Q의 릿지:GBM score 앙상블(고정 50:50)을 기존 dense 특징(코드/수식/제약/장문 마커)으로 나눈 5개 도메인 그룹마다 다른 혼합비율 `w_domain`으로 대체(cost 쪽은 Q 그대로 유지). 좌표하강법(coordinate ascent, 인구 큰 도메인부터)으로 `w_domain`을 그리드서치, 매 단계 로버스트니스(리샘플 10/10)를 1순위 필터로 사용해 P의 실패(로버스트니스 미검증)를 반복하지 않도록 설계 | dev(10/10/10 서브셋 검증됨) | 0.671023 / (safety=0.96) | 0.696591 / (safety=0.90) | 0.742330 / (safety=0.85) | 0.700085 | | **1차 비교(Q의 기존 safety_ratio 0.985/0.9125/0.85 그대로 사용)에서는 +0.000483 개선처럼 보였지만, 이건 착시였음** — 그 비교에서 fast가 10/10이 아니라 9/10만 통과했다는 걸 놓친 것(Q의 safety_ratio는 원래 uniform 0.5 블렌딩용으로 튜닝된 값이라 도메인 게이팅으로 바뀐 score 분포엔 안 맞았음). 도메인 게이팅 자체의 safety_ratio(fast 0.96/balanced 0.90/premium 0.85, 좌표하강법 마지막 단계에서 재탐색)로 10/10/10을 다시 맞춰서 **공정 비교**하니 0.700085 vs Q 0.700170으로 **-0.000085(잡음 수준 무승부)**. 오늘 계속 강조해온 "로버스트니스부터 동일 기준으로 맞춰야 공정 비교"가 여기서 실제로 착시를 잡아낸 사례. 발견된 도메인별 가중치는 code=0.6(릿지쪽 우세)/math_reasoning=0.4/multi_constraint=0.0(GBM 전적으로 신뢰, 다만 n=19로 표본 작음)/general=0.4/long_context=해당 문항 Dev에 0건이라 의미 없음(0.5로 되돌림). **채택 안 함** — 파라미터를 최소화한 마지막 "가벼운" 축까지 시도했지만 순이득 없음, 예측 정확도 개선 방향은 이걸로 최종 종료. 코드는 `build/domain-gated-blend/run.py`(worktree 로컬, 재현 가능). |
| DD | 2026-08-20 | 실험DD: **score를 이항분포 GLM(로지스틱 링크)으로 모델링**. outcomes.json 직접 확인 결과 score는 [0,1] 연속값이 아니라 `num_generations`(2 또는 4)번 생성 중 성공 비율 k/n(값이 정확히 {0, 0.25, 0.5, 0.75, 1} 다섯 개뿐, Train 5,280건 전부 k=round(score×n) 정수 확인됨) — 지금까지(H~CC) 전부 이걸 임의의 연속값처럼 MSE로 학습해서 (1) p 근처 이질분산과 (2) n=4 관측치(738건)가 n=2(4,542건)보다 신뢰도 높다는 걸 무시했음. 각 (episode, model) 관측치를 k개 성공(y=1)+(n-k)개 실패(y=0) 베르누이 시행으로 펼쳐서 `sklearn.linear_model.LogisticRegression`(L2, C는 5-fold episode-group CV로 선택)을 돌리면 n이 큰 관측치가 더 많은 행을 차지해 자연히 더 큰 가중치를 받는 가중 이항GLM과 수학적으로 동치. cost head는 안 건드림 | dev(전부 10/10 서브셋 검증됨) | **최종 채택 조합**: 0.669602(safety=0.98) | 0.698580(safety=0.90) | 0.746023(safety=0.85, 불변) | **0.701222** | | **채택(2026-08-21부로 LL에 대체됨, 아래 참고).** (참고: raw `final_score` 역대 기록 자체는 여전히 N의 0.704659가 더 높지만, N은 리샘플 검증에 실패해 못 쓰는 값 — SOTA는 2026-08-20부로 "실제 채택된 정책" 기준으로 재정의됨, 문서 상단 안내 참고.) 탐색 경과: (1) 순수 이항GLM 단독(cost는 H의 릿지 유지)은 가중합 0.692756으로 H(0.692075) 대비 근소 개선처럼 보였지만 tier별로는 완전히 갈림 — **Fast +0.0128(강한 개선), Premium −0.0142(뚜렷한 악화)**, Balanced 거의 동일. 선택된 C가 0.001~0.003으로 매우 강한 정규화라 극단 확률(0/1 근처)이 중앙으로 눌려서, 확신이 중요한 Premium(light/think 격차 큰 문항)엔 불리하고 애매한 경계가 많은 Fast엔 유리했던 것으로 추정. (2) K의 "두 독립 모델 평균=노이즈 상쇄" 원리를 재적용해 릿지+이항GLM 50:50 블렌드 시도 — Fast 이득은 유지하고 Premium 손실은 -0.0009까지 거의 회복, 가중합 0.698551(H 대비 **+0.0065**, 오늘 최대 폭). (3) Q의 실제 cost 모델과 결합해 릿지+GBM+이항GLM 균등 3-way 블렌드 시도 — 오히려 Q(0.700170)보다 낮음(0.695568, GBM이 이항GLM 장점을 희석). (4) GBM 없이 릿지+이항GLM만 Q의 cost 모델과 결합 — 0.698665로 여전히 Q 미달(Premium이 다시 -0.0085로 악화). (5) **핵심 통찰**: 이항GLM은 Fast/Balanced에 강하고 GBM은 Premium에 강하다는 패턴이 일관됐으므로, **tier마다 다른 score 앙상블**을 쓰기로 — Fast/Balanced는 릿지+이항GLM, Premium은 Q 그대로(릿지+GBM). Q의 cost 모델·safety_ratio 그대로 재사용한 1차 검증에서 가중합 **0.701222**(Q 대비 +0.001052), 전 tier 10/10 통과 확인. **함정 발견 및 수정**: 프로덕션 코드에 통합해 네이티브 self-check를 돌렸더니 **기존 저장된 safety_ratio(fast=0.985/balanced=0.9125, 옛 릿지+GBM 모델용으로 캘리브레이션됨)를 그대로 쓴 채 0.702131이라는 더 높은 값**이 나왔는데, 이걸 곧바로 10/10 리샘플 재검증했더니 **fast 9/10·balanced 9/10로 로버스트니스 실패** — 옛 safety_ratio가 새 score 모델에 우연히 맞지 않는 조합에서 raw Dev-880 점수만 더 높게 나온 경우(실험CC와 정확히 같은 함정 패턴). `_TIER_SAFETY_RATIOS`를 새로 로버스트니스 검증된 값(fast=0.98, balanced=0.90, premium=0.85 불변)으로 교체해 재검증하니 **0.701222로 정확히 10/10/10 전부 통과** — 이게 최종 채택값. `src/ossp_router/routing_ensemble.py`에 `BinomialGlmArtifact`/`_predict_binomial_glm_score` 추가, `_predict_ensemble_score`가 tier 인자를 받아 premium은 릿지+GBM, 나머지는 릿지+이항GLM을 쓰도록 분기(주석에 근거 명시). 학습 스크립트 `baselines/train_binomial_glm_score.py`, 아티팩트 `src/ossp_router/resources/binomial-glm-score.v1.json`(41KB, 순수 로지스틱 회귀라 작음). `.dockerignore`/`tests/test_repository_policy.py` 갱신. linux/arm64·amd64 이미지 빌드 확인, amd64는 실제 컨테이너(read-only/non-root/network none)로 Dev 880문항 라우팅 실행까지 검증(self-check 재현 완전 동일 0.701221590909, 출력 파일 바이트 단위 일치). 기존 테스트 회귀 없음(pre-existing 실패 목록과 정확히 일치). **2026-08-20부로 실제 제출 컨테이너 채택 정책(Q 대체).** `tools/check_runtime.py` 정밀검증은 여전히 미실행 — 제출 전 Linux 환경에서 필요. |
| EE | 2026-08-20 | 실험EE: **DD의 이항GLM score head에 배깅(bootstrap aggregating) 적용**. Train의 부트스트랩 재표본(복원추출, 1,760개씩) B=20개로 각각 이항GLM을 재학습(C는 DD가 이미 CV로 선택한 값 재사용 — 배깅의 핵심은 표본 재구성이지 하이퍼파라미터 재탐색이 아니므로)해서, 20개 예측 확률의 평균을 최종 score로 씀. Fast/Balanced는 릿지+배깅이항GLM, Premium은 DD와 동일하게 릿지+GBM 유지(cost 모델도 Q 그대로) | dev(10/10/10 서브셋 검증됨) | 0.666477(safety=0.98) | 0.698864(safety=0.90) | 0.746023(safety=0.85, 불변) | 0.700057 | | **DD(단일 이항GLM, 0.701222)보다 오히려 낮음(-0.0012)** — Fast가 -0.0031로 특히 나빠짐, Balanced는 +0.0003로 잡음 수준 개선. 원인 추정: 배깅(복원추출)은 재표본마다 원본의 약 63.2%만 커버해 정보 손실이 있는데, DD의 이항GLM은 이미 C=0.001~0.003(강한 L2)으로 저분산 상태라 배깅이 줄일 분산 여유가 크지 않아 정보 손실 비용이 이득을 넘어선 것으로 보임. K/Q의 성공(릿지+GBM, GBM+릿지quantile)은 "동일 모델을 재표본"이 아니라 "서로 다른 편향을 가진 이종 모델의 평균"이었다는 게 핵심이었는데, 배깅은 표본만 바꾼 동일 모델이라 그 효과가 재현 안 됨. **채택 안 함.** 추가로 "이항GLM만 배깅"이 아니라 **릿지+이항GLM 블렌드 전체를 매 bag마다 같이 학습**(bag당 릿지 alpha=1000 + 이항GLM을 같은 부트스트랩 재표본으로 학습해 그 bag의 blend를 만들고, B=20개 blend를 평균)하는 변형도 시도 — 이번엔 방향이 반대로 나와서 fast −0.0014·balanced **+0.0028**·premium 불변, 가중합 **0.701506**로 DD(0.701222) 대비 **+0.000284 근소 개선**(10/10/10 검증됨). 다만 이득이 DD가 Q를 이긴 폭(+0.0011)의 1/4 수준으로 작은데, 추론 비용은 20배(bag 20개×모델 2개=40개를 매 episode마다 계산)로 뛰어 이득 대비 복잡도가 안 맞는다고 판단. **이 조합도 채택 안 함**, 기록만 남김. cost head 배깅(GBM이라 계산비용 훨씬 큼)도 같은 이유로 기대난망이라 판단해 시도하지 않음, GP 회귀 추가도 "score는 이항분포"라는 DD의 핵심 발견과 상충돼(GP는 다시 score를 연속 가우시안으로 취급) 시도하지 않음 — 배깅 계열 탐색 전체를 여기서 종료. 코드는 `build/bagged-binomial/{train.py,train_blend.py}`(worktree 로컬), 아티팩트 `artifacts/bagged-binomial-glm-score.v1.json`, `artifacts/bagged-ridge-binomial-blend.v1.json`(1.1MB). |
| FF | 2026-08-20 | 실험FF: **cost head를 log(cost) 직접 예측 대신 log(input_tokens)+log(output_tokens) 개별 예측으로 분해**. `configs/routing-policy.v1.json` 확인 결과 `fixed_cost=0`이라 `cost = input_tokens×input_rate/1e6 + output_tokens×output_rate/1e6`로 정확히 분해됨. 가설: input_tokens는 프롬프트 길이에서 거의 결정론적으로 나오는 값이라 예측이 쉽고, output_tokens(특히 axk1-think의 추론 과정 길이, Train 기준 표준편차 8,670으로 세 모델 중 압도적으로 큼)만 진짜 어려운 타겟이라 분리하면 회귀가 더 쉬워질 것. hash+regex 270차원 특징으로 6개 헤드(모델×{log input, log output}) 릿지 학습, alpha는 5-fold CV로 선택(1 선택됨 — H의 log-cost 회귀가 선택한 1000보다 훨씬 가벼운 정규화, 개별 타겟이 덜 노이즈끼는 신호라는 정황). score head는 H와 동일 유지, cost head만 비교 | dev(10/10 서브셋 검증됨) | 0.656534 / (safety=0.86) | 0.692045 / (safety=0.80) | 0.725852 / (safety=0.6625) | 0.687983 | | **H(0.655682/0.693182/0.739489, 가중합 0.692075)에 뒤짐(-0.0041)**, Premium이 특히 나빠짐(-0.0136). 이 cost 모델이 필요로 하는 safety_ratio도 H보다 훨씬 낮아(0.86/0.80/0.6625 vs 0.91/0.925/0.85) — 예측 자체의 신뢰도가 더 낮다는 신호. 원인 추정: input_tokens·output_tokens 개별로는 상대오차가 더 낮을 수 있어도, 두 개의 독립적인 예측오차를 곱해서(`exp(log_input)×rate + exp(log_output)×rate`) 합치는 재구성 과정에서 오차가 서로 상쇄되지 않고 복합돼, 단일 log(cost) 직접 예측(오차 소스 1개)보다 최종 재구성값의 분산이 오히려 커진 것으로 보임 — "더 해석 가능한 타겟으로 분해"가 반드시 "최종 오차 감소"로 이어지진 않는다는 사례. **채택 안 함.** 코드는 `build/token-count-cost/train.py`(worktree 로컬), 아티팩트 `artifacts/token-count-cost-ridge.v1.json`. |
| GG | 2026-08-20 | 실험GG: **GBM score head에 배깅 적용**(Premium에만, DD의 릿지+GBM 앙상블 중 GBM 부분만 교체). EE(릿지·이항GLM 배깅)는 "이미 강한 L2/C로 저분산인 선형모델"에 배깅을 걸어 효과가 미미했는데, "배깅의 교과서적 대상은 분산이 크고 편향이 낮은 트리 모델(랜덤포레스트가 하는 일 그 자체)"이라는 지적을 반영해 GBM에 시도. Train 부트스트랩 재표본 B=8개(계산비용 고려해 EE의 20개보다 적게)로 `GradientBoostingRegressor`(n_estimators=150/max_depth=3/learning_rate=0.05, 기존 gbm.v1.json과 동일 파라미터)를 재학습해 평균. Fast/Balanced는 DD 그대로(릿지+이항GLM) | dev(10/10/10 서브셋 검증됨) | 0.669602(safety=0.98, 불변) | 0.698580(safety=0.90, 불변) | 0.728693(safety=0.7375) | 0.696023 | | **DD(0.701222)보다 뚜렷이 나쁨(-0.0052)** — Premium이 -0.0173으로 크게 악화(Fast/Balanced는 안 건드려서 불변). "트리 모델=고분산=배깅이 교과서적으로 맞다"는 사전 판단이 랜덤포레스트에는 맞지만 **GBM(gradient boosting)에는 그대로 적용되지 않음이 확인됨** — 랜덤포레스트는 깊고 제약 없는 불안정한 개별 트리를 평균 내 안정화하려고 배깅을 쓰지만, GBM은 얕은 트리(depth=3)+낮은 학습률(0.05)+순차적 잔차적합으로 부스팅 자체가 이미 분산을 관리하는 다른 방식의 앙상블이라 배깅을 얹는 게 이질적으로 작용. 게다가 각 bag이 원본의 약 63.2%만 커버하는 정보손실이 Premium처럼 극단적 확신이 중요한 결정에서 특히 크게 작용한 것으로 추정. **채택 안 함.** 이걸로 배깅을 선형모델(EE, 미미~근소개선)과 트리모델(GG, 뚜렷한 악화) 양쪽 다 시도했고 둘 다 실익이 없거나 손해라 배깅 계열 탐색을 최종 종결. 코드는 `build/bagged-gbm/train.py`(worktree 로컬), 아티팩트 `artifacts/bagged-gbm-score.v1.json`. |
| HH | 2026-08-20 | 실험HH: **Elastic Net(L1+L2)으로 score/cost head 학습** — 지금까지(H~GG) 전부 릿지(L2 전용)만 썼는데, 270차원(14 dense + 256 해시버킷) 중 불필요한 해시버킷을 0으로 만드는 L1 특징선택 효과 검증 안 됨. `sklearn.linear_model.MultiTaskElasticNetCV`로 6개 타겟(score×3+log-cost×3)이 **같은 희소성 패턴**(어떤 해시버킷을 켤지 헤드끼리 공유)을 갖게 학습, alpha·l1_ratio는 5-fold CV로 선택(l1_ratio=0.99 선택 — 거의 순수 L1) | dev(10/10 서브셋 검증됨) | 0.651705(safety=0.87) | 0.689489(safety=0.90) | 0.725000(safety=0.7375) | 0.685029 | | **H(가중합 0.692075)에 3개 tier 전부 뒤짐(-0.0070)**, Premium이 -0.0145로 특히 나쁨(GG의 GBM 배깅 실패와 비슷한 패턴 — Premium은 극단적 확신이 중요한데 정규화 방식을 바꾸는 실험마다 유독 여기서 크게 깎임). 실제로 47개 특징(dense 3개+해시버킷 44개, 270개 중 17%)이 정확히 0으로 눌림 — L1이 의도대로 작동은 했지만, 그 희소성이 예측 정확도 개선으로 이어지진 않음. 270차원 중 44개 해시버킷을 0으로 만드는 정도로는 원래 해시충돌·노이즈가 문제였다고 보기 어렵고(alpha=1000의 강한 L2로도 이미 충분히 정규화되고 있었다는 뜻), 오히려 일부 약하지만 유효했던 신호까지 같이 잘려나간 것으로 추정. **채택 안 함.** 코드는 `build/elasticnet/train.py`(worktree 로컬), 아티팩트 `artifacts/elasticnet.v1.json`. |
| II | 2026-08-20 | 실험II: **DD의 "이항GLM이 Premium에 약한 이유=정규화가 너무 강함(C 작음)" 진단을 직접 검증**. DD의 C 선택은 로그가능도(proper scoring rule) 최대화 기준 CV였는데, 이게 라우팅 결정 품질과 반드시 일치하진 않을 수 있다는 가설로 C∈{0.001, 0.003, 0.01, 0.03, 0.1, 0.3, 1.0, 3.0, 10.0, 30.0, 100.0} 11개 값 전부 학습, 각각 릿지+이항GLM(C) 블렌드로 **Premium 전용** 로버스트니스 우선 평가(fast/balanced는 안 건드림, DD 그대로) | dev(10/10 서브셋 검증됨, 11개 C값 전부) | — (fast/balanced 불변, DD와 동일) | — | **11개 C 전부 GBM(0.746023) 미달** — 최고는 C=0.003(0.739489, -0.0065), C를 100까지 키워도 개선은커녕 0.72대로 오히려 나빠짐(C≥1에서 0.72~0.73 사이 정체) | — | | **진단 자체가 틀렸음이 확인됨** — "정규화가 너무 강해서"가 아니라 **이항GLM(로지스틱, 선형 결정경계)이 Premium의 결정 패턴을 원천적으로 못 잡는 것**으로 결론이 바뀜. GBM(트리 기반)이 잡아내는 비선형 상호작용·임계값 구조를 로지스틱 회귀는 애초에 표현할 수 없어서, C를 아무리 조정해도(정규화 강도만 바뀌지 모델의 함수형태 자체는 선형인 채로 남음) 격차가 안 좁혀진 것으로 보임 — Fast/Balanced에서 이항GLM이 이겼던 건 그쪽 결정 경계가 상대적으로 선형에 가까워서였고, Premium은 애초에 비선형 모델(GBM)이 필요한 영역이라는 재해석. **채택 안 함**(DD의 tier별 분업 — Fast/Balanced=릿지+이항GLM, Premium=릿지+GBM — 이 실험으로 오히려 더 확실하게 정당화됨). 코드는 `build/binomial-glm-premium/train.py`(worktree 로컬), 아티팩트 `artifacts/binomial-glm-c-sweep.v1.json`. |
| JJ | 2026-08-20 | 실험JJ: **이항 손실 GBM**(`GradientBoostingClassifier`, 기본 log-loss/deviance) — II("Premium엔 비선형 모델 필요")와 DD("이항분포 신뢰도 반영")를 결합. DD와 동일한 시행 단위 확장 데이터(k개 성공+(n-k)개 실패, 같은 특징행 반복)에 `GradientBoostingClassifier`(n_estimators=150/max_depth=3/learning_rate=0.05, 기존 GBM과 동일 파라미터) 학습, Premium 전용으로 릿지와 앙상블 | dev(10/10 서브셋 검증됨) | — (fast/balanced 불변) | — | 0.743750(safety=0.85) | — | | **DD의 GBM(MSE, 0.746023)에 근소하게 뒤짐(-0.0023)** — 순수 이항GLM(최고 -0.0065, 실험II)보다는 훨씬 격차가 좁혀져서 "비선형 모델이 Premium에 필요하다"는 가설이 재확인됐지만, 이항 손실로 바꾼 것 자체는 아직 순수 MSE 회귀를 못 넘음. 시행 단위 확장(같은 episode가 num_generations만큼 중복 행으로 들어감, 최대 4배)이 트리 분기 학습 방식을 MSE 단일 타깃 회귀와 다르게 만들어서 근소한 손해로 이어진 것으로 추정 — "이론적으로 더 맞는 손실함수"가 근소한 표본 재구성 비용을 상쇄할 만큼 크지 않았던 사례. **채택 안 함.** 코드는 `build/binomial-gbm/train.py`(worktree 로컬), 아티팩트 `artifacts/binomial-gbm-score.v1.json`. |
| KK | 2026-08-20 | 실험KK: JJ의 원인 가설("시행단위 확장으로 episode가 최대 4배 중복 행이 되는 부작용")을 직접 검증 — 행 중복 없이 **episode당 1행을 유지한 채 `num_generations`를 `GradientBoostingRegressor`의 `sample_weight`로 직접 전달**(MSE 손실 그대로, 신뢰도 가중만 순수하게 추가). Premium 전용, 릿지와 앙상블 | dev(10/10 서브셋 검증됨) | — (fast/balanced 불변) | — | 0.744034(safety=0.85) | — | | **DD의 GBM(0.746023) 대비 -0.0020 — JJ(-0.0023)와 사실상 동일**. 행 중복을 완전히 제거했는데도 격차가 그대로라는 건, **JJ의 부진 원인이 행 중복이 아니라 "신뢰도 가중 자체가 GBM엔 별 도움이 안 됨"이라는 뜻** — 서로 다른 두 구현(시행단위 확장 분류기 vs sample_weight 회귀)이 같은 결론에 수렴해 이중으로 확인됨. 추정: 트리 모델은 분기 시 연속값의 정확한 크기보다 상대적 순서/임계값에 의존하는 경향이 있어, 선형모델(릿지·이항GLM)만큼 "관측치별 신뢰도 차이"에서 얻는 이득이 크지 않은 것으로 보임 — DD의 핵심 통찰(이항분포 신뢰도 가중)이 모델 종류를 가리지 않고 보편적으로 통하는 게 아니라 **선형모델에 특히 잘 맞는 통찰**이었다는 재해석. **채택 안 함.** 이걸로 Premium 개선 시도(II/JJ/KK)를 일단락 — GBM(MSE, 가중치 없음)이 Premium에서 시도한 모든 대안보다 여전히 최선. 코드는 `build/weighted-gbm/train.py`(worktree 로컬), 아티팩트 `artifacts/weighted-gbm-score.v1.json`. |
| LL | 2026-08-21 | 실험LL: **Fast/Balanced의 이항GLM C를 tier별로 재탐색**. DD는 fast/balanced가 이항GLM 아티팩트 하나(모델별 C: light=0.001/ax31=0.003/axk1-think=0.003, 로그가능도 CV로 선택)를 공유했는데, 이 C 선택이 로그가능도 최적화 기준이지 라우팅 성능 직접 최적화가 아니라는 점에 착안해 실험II의 C 그리드(0.001~100, 11개)를 재사용해 fast/balanced 각각 로버스트니스 우선으로 재평가(균일 C, 모델 3개 전부 같은 값) | dev(10/10/10 서브셋 검증됨) | 0.669602(safety=0.98, DD와 동일 아티팩트 유지) | 0.699716(safety=0.9375, **균일 C=0.001 전용 아티팩트**) | 0.746023(safety=0.85, 불변) | **0.701562** | ⭐ | **채택.** Fast는 균일 C 그리드 전부 DD의 기존 혼합-C(0.001/0.003/0.003) 아티팩트를 못 넘음(최고 0.660511 vs 실제 0.669602) — DD의 혼합 설정이 이미 fast엔 최적에 가까웠던 것으로 보여 그대로 유지. **Balanced는 세 모델 모두 C=0.001로 통일한 별도 아티팩트가 DD의 혼합-C(0.698580)보다 +0.0011 나음(0.699716)** — balanced 전용 이항GLM 아티팩트를 새로 만들어 채택. 탐색 중 두 가지 방법론적 함정을 실제로 겪음: (1) 처음엔 safety_ratio 17포인트 그리드로 평가했더니 DD의 실제 배포값(fast 0.669602/balanced 0.698580)조차 재현이 안 돼 41포인트로 다시 돌림 — 그래도 fast는 여전히 재현이 안 됐는데, 원인은 그리드 해상도가 아니라 **DD의 진짜 배포 설정이 "모델별로 다른 C"(혼합)였는데 내 스윕 아티팩트는 "3개 모델 전부 같은 C"만 테스트했던 것**(비교 기준 자체 불일치, 실험CC 때와는 다른 종류의 함정). (2) balanced 전용 아티팩트를 프로덕션에 통합한 뒤 처음 native self-check를 돌렸을 때 DD 시절 저장된 safety_ratio(0.90)를 그대로 써서 0.700369(기대보다 낮음)가 나왔는데, 즉시 10/10 리샘플 재검증 없이 넘어가지 않고 확인해 새 아티팩트에 맞는 safety_ratio(0.9375)로 교체 후 재검증 — **DD/CC에서도 반복됐던 "score 모델이 바뀌면 safety_ratio도 반드시 재검증"이 이번에도 정확히 재현됨**. `src/ossp_router/routing_ensemble.py`의 `_predict_ensemble_score`가 balanced에 한해 별도 아티팩트(`binomial_glm_balanced_artifact`)를 쓰도록 분기 추가, `_TIER_SAFETY_RATIOS["balanced"]`를 0.90→0.9375로 교체. 학습 스크립트 `baselines/train_binomial_glm_score_balanced.py`, 아티팩트 `src/ossp_router/resources/binomial-glm-score-balanced.v1.json`(41KB). `.dockerignore`/`tests/test_repository_policy.py` 갱신. linux/arm64·amd64 이미지 빌드 확인, amd64는 실제 컨테이너(read-only/non-root/network none)로 Dev 880문항 라우팅 실행까지 검증(self-check 재현 완전 동일 0.7015625, 출력 파일 바이트 단위 일치). 기존 테스트 회귀 없음(pre-existing 실패 목록과 정확히 일치). **2026-08-21부로 실제 제출 컨테이너 채택 정책(DD 대체).** `tools/check_runtime.py` 정밀검증은 여전히 미실행 — 제출 전 Linux 환경에서 필요. |
